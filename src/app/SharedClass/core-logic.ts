export abstract class CoreLogic {
  StandardDeviation(listofdata: string[]): number {
    //some logic here
    console.log(listofdata);
    let samplenumber = 2;
    return samplenumber;
  }
  Mean(listofArray: number[]): number {
    console.log(listofArray);
    let MeanNumberdummy = 2;
    return MeanNumberdummy;
  }
}
